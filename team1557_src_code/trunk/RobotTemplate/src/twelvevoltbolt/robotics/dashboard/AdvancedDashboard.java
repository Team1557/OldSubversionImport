/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package twelvevoltbolt.robotics.dashboard;

import edu.wpi.first.wpilibj.Dashboard;
import edu.wpi.first.wpilibj.DriverStation;
import edu.wpi.first.wpilibj.IDashboard;

/**
 *
 * @author code
 */
public class AdvancedDashboard {
    public void update() {
        /*
        Dashboard dash = (Dashboard) DriverStation.getInstance().getDashboardPackerInUseLow();
        dash.addCluster();
        dash.addString("This is a custom dashboard!");
        dash.finalizeCluster();
        */
    }
}
